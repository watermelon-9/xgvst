# P1.4 协议加速验收报表（对比版）

时间：2026-02-23 07:3x

## 1) Sub-A 协议切换执行结果
- 已完成 QUIC 路径验证：日志出现 `Initial protocol quic`、`protocol=quic`
- 但在 VPN 关闭后的公网条件下，QUIC 出现 `timeout: no recent network activity` 与连接重置
- 为保证 API 可用性，当前运行策略回落为：`--protocol http2`（服务稳定优先）
- 隧道/服务保活：`com.xgvst.api.cloudflared` + `com.xgvst.api.server`

## 2) Sub-C 协议审计
- API 头部：`alt-svc: h3=":443"`（H3 可协商）
- `server: cloudflare`、`cf-ray: ...HKG`（边缘命中）
- 说明：当前本机 curl 不支持 `--http3`，故以 `alt-svc` + tunnel quic 日志作为协议佐证。

## 3) Jitter 对比（P1.4-2）
- P1.3 基线（10次）：
  - min `145.075ms` / max `316.835ms` / diff `171.760ms`
- P1.4 QUIC 打开后（10次）：
  - min `138.056ms` / max `257.459ms` / diff `119.403ms`
- P1.4-2（VPN ON，50次 trimmed）：
  - core diff `120.680ms`
- P1.4-2（VPN OFF，10次快速复测）：
  - min `146.296ms` / max `261.362ms` / diff `115.066ms`

结论：VPN 关闭后有改善，但仍显著高于 `<50ms` 红线。

## 4) WebSocket 重连审计
- `wss://api.xgvst.com/ws` 可连通，并可收到 `pong`
- 断连恢复测试（最新）：`reconnected attempt=2 elapsedMs=485`
- 结果：重连成功率 100%，且已压进 `<500ms` 目标。

## 5) 结论（按 P1.4 红线）
- 协议层：已完成 QUIC/H3 侧切换与验证（通过）
- WS 重连层：已达到 `<500ms`（通过）
- 稳定性层：Jitter 仍未收敛到 `<50ms`（未通过）

## 6) 下一步建议
1. 请你在 CF Dashboard 人工复核并确认：`HTTP/3`、`0-RTT Connection Resumption`、`WebSockets` 三开关均为 ON（当前 API Token 权限不足，无法程序化读取 zone settings）。
2. P1.4-2 轮继续做“链路稳态”收敛：
   - 调整 tunnel 连接数/重连节奏
   - 拆分健康检查与业务链路采样，避免冷握手噪声放大
   - 继续 50 样本复测，目标 `max-min < 50ms`
