# P1.1 严格验收门禁报告（2026-02-23）

- 项目：`/Users/mac/.openclaw/workspace/xgvst`
- 验收角色：Sub-C（Sentinel）
- 验收口径：严格按「1.1 SvelteKit 工程初始化」5条 DoD，**任一不满足即总体 FAIL**

## 总判定

**FAIL**

失败条目：
- DoD-1 `pnpm dev` 秒开（<800ms）
- DoD-2 Mock 行情支持暗黑切换
- DoD-4 Lighthouse Performance ≥98（移动+桌面）

---

## DoD 明细（含证据命令与结果）

### DoD-1：`pnpm dev` 秒开（<800ms）
**证据命令**
```bash
corepack pnpm dev
```
**关键结果**
```text
VITE v7.3.1  ready in 983 ms
```
另一次同环境复测（127.0.0.1:4173）日志：
```text
VITE v7.3.1  ready in 912 ms
```
**判定：FAIL**（983ms/912ms 均 > 800ms）

---

### DoD-2：Mock行情（全部分块、自选、个股）可正常显示且支持暗黑切换
**证据命令 1（路由+Mock接线）**
```bash
find apps/web/src/routes -maxdepth 3 -type f | sort
grep -n "fetchUniverse\|mockUniverse\|watchlist\|boards" \
  apps/web/src/routes/market/+page.svelte \
  apps/web/src/lib/api/client.ts \
  apps/web/src/lib/api/mock.ts
```
**关键结果**
```text
routes 存在：+page.svelte / market/+page.svelte / detail/[symbol]/+page.svelte
market 页已调用 fetchUniverse，client.ts 在 API_BASE 为空或失败时回退 mockUniverse
mock.ts 含 boards / watchlist 数据
```

**证据命令 2（暗黑切换能力检查）**
```bash
grep -Rni "dark\|theme\|toggle\|prefers-color-scheme" \
  apps/web/src/routes apps/web/src/lib apps/web/src/app.css | head -n 40
```
**关键结果**
```text
无输出（未检出暗黑切换/主题切换实现）
```
**判定：FAIL**（Mock 数据链路存在，但“支持暗黑切换”证据不足且代码未实现可见切换能力）

---

### DoD-3：Svelte5 Runes 与 UnoCSS 全局变量生效、无编译警告
**证据命令 1（类型与编译告警）**
```bash
corepack pnpm --filter web check
```
**关键结果**
```text
svelte-check found 0 errors and 0 warnings
```

**证据命令 2（构建）**
```bash
corepack pnpm --filter web build
```
**关键结果**
```text
✓ built in 328ms（client）
✓ built in 1.52s（server）
> Using @sveltejs/adapter-cloudflare
  ✔ done
```

**证据命令 3（Runes + UnoCSS 变量）**
```bash
# Runes
cat apps/web/src/lib/runes/market-state.svelte.ts
# Uno 颜色变量
cat apps/web/uno.config.ts
# 全局 CSS 变量
cat apps/web/src/app.css
```
**关键结果**
```text
market-state.svelte.ts 使用 $state
uno.config.ts 预置 up/down/bgPurple
app.css 定义 --color-up/--color-down/--color-bg-purple
```
**判定：PASS**

---

### DoD-4：Lighthouse Performance ≥98（移动+桌面）
**证据命令**
```bash
npx -y lighthouse http://127.0.0.1:4173/ \
  --only-categories=performance \
  --chrome-flags='--headless=new --no-sandbox' \
  --output=json --output-path=reports/lighthouse/p1.1-gate-mobile.json

npx -y lighthouse http://127.0.0.1:4173/ \
  --only-categories=performance --preset=desktop \
  --chrome-flags='--headless=new --no-sandbox' \
  --output=json --output-path=reports/lighthouse/p1.1-gate-desktop.json
```
**关键结果（由 JSON 解析）**
```text
mobile: 87
desktop: 93
```
**判定：FAIL**（移动/桌面均 < 98）

---

### DoD-5：`PERFORMANCE.md` 和初始报告已提交
**证据命令**
```bash
git ls-files PERFORMANCE.md reports/lighthouse
git log --oneline -- PERFORMANCE.md | head
git log --oneline -- reports/lighthouse | head
```
**关键结果**
```text
PERFORMANCE.md 已被跟踪
reports/lighthouse 下初始/审计报告已被跟踪
存在提交记录：
- 9510313 docs(perf): establish P1.1 baseline and centralize report index
- 5c586e1 audit(p1.1): add baseline compliance report and lighthouse evidence
```
**判定：PASS**

---

## 门禁结论（严格）

- 通过：2/5
- 失败：3/5
- **总体：FAIL**

> 按门禁规则：任一 DoD 不满足即 FAIL；本次存在 3 项不满足，故拒绝放行。
