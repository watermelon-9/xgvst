# P2.1 行情多源接入（开工进展）

时间：2026-02-24

## 已完成（首轮）
- 新增数据源目录与实现：
  - `packages/workers/src/sources/QuoteSource.ts`
  - `BaseMockSource.ts`
  - `AllTickSource.ts` / `SinaSource.ts` / `EastMoneySource.ts` / `TencentSource.ts`
- 新增 `SourceManager`：
  - 优先级顺序：AllTick → Sina → EastMoney → Tencent
  - 支持 3s 内故障转移（当前实现 1s 触发重连）
- Workers 主入口接入连接池：
  - 全局 WebSocket 连接池
  - 心跳保活（15s ping，30s 超时清理）
  - 订阅合并（仅聚合当前客户端订阅 symbols）
- 新增接口：
  - `GET /api/source/status`
  - `GET /ws/quote`（前端可直接订阅）

## 验证结果
- `pnpm --filter workers check` ✅
- `wrangler deploy` ✅（Version: `5c9a351c-d40a-47be-a1b0-c693a8881889`）
- `GET /api/source/status` 返回 activeSource=`alltick` ✅
- WebSocket 订阅测试收到实时 tick（symbols: 000001/600519）✅

## 下一步（P2.1继续）
1. 接入真实源鉴权占位（ALLTICK_TOKEN/SINA_COOKIE/EASTMONEY_TOKEN/TENCENT_TOKEN）与合规开关。
2. 增加源级失败注入与自动切换压测脚本（验证 3s 内切换）。
3. 前端 `$lib/api` 新增 `useQuoteWebSocket` 对接 `/ws/quote`。
4. 输出 P2.1 性能初报（主源延迟、切换耗时、重连成功率）。
