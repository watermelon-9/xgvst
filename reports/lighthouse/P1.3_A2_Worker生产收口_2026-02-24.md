# P1.3-A2 Worker生产收口报告（2026-02-24）

## 1. 范围与硬约束核对

- 范围：仅在 `xgvst/` 内处理。
- Worker 位置：`packages/workers`（独立包）✅
- `wrangler.toml`：包含 Durable Object 绑定 + migrations ✅
- proto 编码：保留，且改为 Worker 安全可运行实现（无 `eval`）✅
- `compatibility_date`：使用当前 Wrangler 可支持的最新日期 `2026-02-23`（`2026-02-24` 会被判定 future/unsupported）✅
- Hono：单 Worker 入口（`src/index.ts`）✅

## 2. 生产收口改动

### 2.1 Workers 配置

文件：`packages/workers/wrangler.toml`

- `compatibility_date = "2026-02-23"`
- Durable Object 迁移改为 Free 计划可用形态：
  - `new_sqlite_classes = ["QuoteDurableObject"]`
- 资源绑定更新为真实云端资源：
  - KV `QUOTE_KV` id: `e52352ff583f4ebdb6c8256f2a97c931`
  - KV preview_id: `a10795c3cd464308883ce1f48ff2281a`
  - D1 `QUOTE_DB` database_id: `9db19911-21e7-4245-8c94-6571aa7a35fb`

### 2.2 安全收口（密钥/环境）

- 从 Worker 代码与 `wrangler.toml` 移除明文业务变量，统一改为：
  - Worker 密钥走 `wrangler secret put`（示例：`QUOTE_API_TOKEN`）
  - Pages 环境变量在文档中固化（见下）
- 文件：`deploy/cloudflare-pages-baseline.md`
  - 新增 Pages/Workers 环境变量与密钥管理基线
  - 明确“仓库禁止提交真实密钥”

### 2.3 proto 编码保留并修复 Worker 运行兼容

文件：`packages/workers/src/proto/quote.ts`

- 保留 protobuf 编码能力
- 将 `protobufjs/light` 反射式编码（触发 `Code generation from strings disallowed`）
  替换为 `protobufjs/minimal` + `Writer` 静态写入字段，避免 Worker 禁用 `eval` 的运行时错误。

## 3. 执行记录

### 3.1 workers check

命令：

```bash
corepack pnpm workers:check
```

结果：通过（`wrangler types && tsc --noEmit` 成功）。

### 3.2 wrangler deploy --dry-run

命令：

```bash
cd packages/workers
npx wrangler deploy --dry-run
```

结果：通过。

### 3.3 deploy（可行时执行）

先后处理问题：
1. 初次失败：KV namespace 占位符不存在 → 已创建并回填真实 KV/D1。
2. 再次失败：Free 计划 DO 迁移要求 `new_sqlite_classes` → 已修正 migration。
3. 最终部署成功。

成功输出：

- Worker URL: `https://xgvst-workers.viehh642.workers.dev`
- Version ID: `035efad8-afb8-4b5f-a987-3beedcfcf0b9`

## 4. wrangler dev 验证

命令：

```bash
cd packages/workers
npx wrangler dev --port 8791 --ip 127.0.0.1 --local
```

### 4.1 `/health`

- 请求：`curl -i http://127.0.0.1:8791/health`
- 结果：`HTTP/1.1 200 OK`

### 4.2 `/api/quote/mock`

- 请求：`curl -i http://127.0.0.1:8791/api/quote/mock`
- 结果：`HTTP/1.1 200 OK`
- 返回包含 `protoBase64`（验证 proto 编码链路可用）

### 4.3 `/ws/quote` 升级

- 请求：WebSocket Upgrade Header（curl）
- 结果：`HTTP/1.1 101 Switching Protocols`

## 5. 备注

- `compatibility_date` 已尽量取新；`2026-02-24` 当前 Wrangler 校验为 future/unsupported，故落地为 `2026-02-23`。
- 本次已完成 Worker 生产收口并落地可访问 URL。
