# P1.1-FIX 对齐修复报告（修订版主线）

日期：2026-02-23  
仓库：`/Users/mac/.openclaw/workspace/xgvst`

## 1. 修复目标
按修订版主线对 P1.1 偏差进行纠偏，确保：
1) 保留并启用 UnoCSS（含 `up/down/bgPurple` 全局主题色）  
2) 不破坏现有路由与 `$lib/api/mock`  
3) 启动性能尽可能优化  
4) 重新执行 `check/build/dev ready` 基准

---

## 2. 本次修复改动

### 2.1 UnoCSS 重新启用（已恢复）
- `apps/web/vite.config.ts`
  - 恢复 `UnoCSS()` 插件（此前被移除）
  - 当前为：`plugins: [UnoCSS(), sveltekit()]`
- `apps/web/src/routes/+layout.svelte`
  - 增加 `import 'virtual:uno.css';`，确保 UnoCSS 产物进入应用
- `apps/web/uno.config.ts`
  - 保持并生效金融主题色：
    - `up: #ef4444`
    - `down: #22c55e`
    - `bgPurple: #4f46e5`

### 2.2 全局颜色变量落地（双保险）
- `apps/web/src/app.css`
  - 新增 CSS 变量：
    - `--color-up: #ef4444`
    - `--color-down: #22c55e`
    - `--color-bg-purple: #4f46e5`
  - `.up/.down/body` 改为引用变量，避免散落硬编码。

### 2.3 保持路由与 API 抽象不破坏
- 路由仍为：
  - `/`
  - `/market`
  - `/detail/[symbol]`
- `$lib/api/mock.ts`、`$lib/api/client.ts` 未变更接口行为。

### 2.4 轻量性能对齐
- 未引入重型 UI 库；仅恢复 UnoCSS 必要能力。
- 将部分页面内联样式改为 class（降低样式散落、便于 UnoCSS 收敛）：
  - `routes/market/+page.svelte`
  - `routes/detail/[symbol]/+page.svelte`
  - `routes/+page.svelte` 增加 Uno 主题类实际使用点（验证 `bgPurple/up/down` 生效）

---

## 3. 重新基准结果

### 3.1 `pnpm --filter web check`
- 结果：✅ 通过
- `svelte-check found 0 errors and 0 warnings`

### 3.2 `pnpm --filter web build`
- 结果：✅ 通过
- client build: `built in 380ms`
- server build: `built in 1.62s`

### 3.3 `pnpm --filter web dev` ready（5 次）
测试方法：启动后捕获 Vite `ready in xxx ms`，就绪即终止进程。  
结果（ms）：
- ready: `[650, 629, 595, 606, 581]`
- wall: `[805, 792, 763, 781, 765]`
- 平均：
  - ready `612.2ms`
  - wall `781.2ms`

结论：在恢复 UnoCSS 前提下，`dev ready` 平均仍满足 `<800ms` 目标。

---

## 4. 对齐结论
- [x] UnoCSS 已保留并启用
- [x] `up/down/bgPurple` 主题色保留且已被页面消费
- [x] 路由与 `$lib/api/mock` 未破坏
- [x] `check/build/dev ready` 已重跑并给出基准

---

## 5. 风险与后续建议

### 已知风险
1. 恢复 UnoCSS 后，极冷启动首轮在低性能机器上可能高于当前均值（本机当前稳定在 ~600ms ready）。
2. 如后续大量增加动态 class（字符串拼接），需注意 Uno 扫描命中策略，避免样式遗漏。

### 建议
1. 保持 Uno 类名尽量静态可分析；动态颜色走主题 token。
2. 继续维持 `PUBLIC_ENABLE_MSW` 默认关闭策略，避免 dev 首屏被非关键 mock 启动链路影响。
