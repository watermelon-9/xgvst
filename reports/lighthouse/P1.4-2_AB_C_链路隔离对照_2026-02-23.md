# P1.4-2 A/B/C 链路隔离对照（进行中）

> 日期：2026-02-23  
> 主控：Main Agent  
> 执行：Sub-A（测试） / Sub-C（记录）

## 统一口径
- 目标接口：`https://api.xgvst.com/v3/health`
- 样本数：50
- 判定：trimmed diff `< 50ms` 才算通过
- 采样脚本：`scripts/sentry_jitter_trimmed_check.sh`

---

## A组（当前宽带 + TCP 隧道 http2）
- cloudflared edge：`1xlax01, 1xlax09`（LAX）
- 50次结果：
  - `samples=50, trim_each_side=2, core=46`
  - `avg=1824.530ms`
  - `min=1198.730ms`
  - `max=3658.243ms`
  - `diff=2459.513ms`
- 判定：**FAIL**

### A组补充观测
- 存在极端尖峰：`sample#6 = 11510.574ms`
- traceroute（ICMP）对 `api.xgvst.com` 与 cloudflare edge IP 均为 `*`，当前网络下不可作为主证据。

---

## B组（低MTU探测，macOS 实际使用 MTU=1280）
- cloudflared edge：`1xlax01, 1xlax06, 2xlax09`（LAX）
- 50次结果：
  - `samples=50, trim_each_side=2, core=46`
  - `avg=1861.054ms`
  - `min=1192.580ms`
  - `max=3264.446ms`
  - `diff=2071.866ms`
- 判定：**FAIL**
- 说明：macOS Wi‑Fi MTU 合法范围 `1280-1500`，无法设置 1200，因此采用 1280 作为低MTU替代探测。

---

## C组（手机 5G 热点）
- 状态：待执行
- 备注：将切换到 5G 热点后，按同口径立即跑 50 次。

---

## 当前结论（阶段）
1. A/B 两组都失败，且均为 LAX 边缘，diff 仍在 `2s` 量级。
2. 低MTU（1280）未显著改善，说明主因并非单纯 MTU。
3. 关键待验证项只剩 C 组（5G 热点）；若 C 组明显改善，可基本锁定为家庭宽带出口路径问题。
