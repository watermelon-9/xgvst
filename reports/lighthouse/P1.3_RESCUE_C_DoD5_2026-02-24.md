# P1.3-RESCUE-C｜DoD5 救援复验报告（2026-02-24）

范围：仅在 `xgvst` 内操作。  
目标：按《修订版》四段输出，对 DoD5（冷启动<50ms证据链、Lighthouse无新下降）给出逐条 PASS/FAIL。

---

## 第一段｜详细步骤（对应《修订版》1）

### 1) 按同口径重跑 Lighthouse（已执行）

**目标域名（强制口径）**：`https://xgvst.com/`  
- mobile：5 次
- desktop：2 次

**替代对照（可控范围）**：`https://xgvst-web.pages.dev/`  
- mobile：5 次
- desktop：2 次

原始结果目录：
- `reports/lighthouse/P1.3_RESCUE_C_DoD5_2026-02-24/raw/`

汇总文件：
- `reports/lighthouse/P1.3_RESCUE_C_DoD5_2026-02-24/summary.json`

### 2) 中位数结果（本轮）

#### A. xgvst.com（验收主口径）
- **Mobile 5次分数**：78 / 92 / 85 / 94 / 95  
  - **中位数：92**
  - TBT 中位数：**363ms**
  - LCP 中位数：**1127.1ms**
- **Desktop 2次分数**：100 / 100  
  - **中位数：100**
  - TBT 中位数：**23.5ms**
  - LCP 中位数：**455.9ms**

#### B. pages.dev（替代对照）
- **Mobile 5次分数**：100 / 100 / 100 / 100 / 100  
  - **中位数：100**
  - TBT 中位数：**0ms**
  - LCP 中位数：**1090.1ms**
- **Desktop 2次分数**：100 / 100  
  - **中位数：100**
  - TBT 中位数：**0ms**
  - LCP 中位数：**321.1ms**

### 3) 与 P1.2 基线对比（“无新下降”口径）

基线（沿用 P1.2 最终复验）：
- mobile 中位数：**97**（96/98/98/97/95）
- desktop 中位数：**100**（100/100）

对比：
- xgvst.com mobile：**92 < 97（下降）**
- xgvst.com desktop：**100 = 100（持平）**

---

## 第二段｜注意事项与下降项定位（对应《修订版》2）

### 1) 下降项定位（优先 TBT/LCP）

在 xgvst.com mobile 样本中，`cdn-cgi/challenge-platform/scripts/jsd/main.js` **每次均出现（5/5）**，且与 TBT 波动一致：

- 低分样本（run1，score 78）：
  - TBT = 969ms
  - `jsd/main.js` scripting ≈ **2036.684ms**
  - 主线程 scriptEvaluation ≈ **2117.46ms**
- 相对高分样本（run5，score 95）：
  - TBT = 256ms
  - `jsd/main.js` scripting ≈ **609.048ms**

对照 pages.dev mobile：
- `jsd/main.js` 出现次数 **0/5**
- TBT 中位数 **0ms**
- 分数 5/5 为 **100**

结论：本轮下降核心由 **Cloudflare JSD 注入导致的主线程脚本开销抬升（TBT）**，并连带拖低移动端总分；业务代码本身在 pages.dev 对照下未见退化。

### 2) “可执行的最小操作清单”（用于消除 JSD 干扰并复验）

按最小变更优先，建议执行以下任一组：

1. **临时测试白名单（推荐）**
   - 在 Cloudflare WAF / Security Rules 新建规则：对测试出口 IP + `host = xgvst.com` 生效；
   - 动作选择 `Skip`，跳过 JS Challenge / Bot 相关检测；
   - 生效后立刻按同口径重跑 mobile 5 次 + desktop 2 次。

2. **短时关闭 JavaScript Detections（仅测试窗口）**
   - 在 Cloudflare 安全设置中短时关闭 JSD；
   - 复验完成后立即恢复。

3. **固定测试通道**
   - 固定一台测试机+固定出口 IP；
   - 配合上面第1条形成可重复基线，避免随机挑战注入。

> 本轮已在可控范围采用 pages.dev 对照，证明业务代码无退化；若要让 xgvst.com 达到“无新下降”，需先执行上面最小清单之一再复验。

---

## 第三段｜工作安排执行回执（对应《修订版》3）

1. 已完成 xgvst.com 同口径复测：mobile 5 次、desktop 2 次。  
2. 已完成 pages.dev 对照复测：mobile 5 次、desktop 2 次。  
3. 已生成机器可审计汇总：`summary.json`（含每次分数、TBT/LCP、jsd 出现情况）。  
4. 已完成下降项定位并给出可执行最小操作清单（优先 TBT/LCP）。

---

## 第四段｜DoD5 验收判定（对应《修订版》4）

### 子项A：冷启动 < 50ms 证据链
- 证据沿用：`reports/lighthouse/P1.3_C2_CI/wrangler-tail-xgvst-workers-ok-20260224-0031.jsonl`
- 关键值：`wallTime=3ms`、`cpuTime=3ms`（<50ms）
- 判定：**PASS**

### 子项B：Lighthouse 无新下降（xgvst.com 主口径）
- 基线：mobile 中位数 97；desktop 中位数 100
- 本轮：mobile 中位数 92；desktop 中位数 100
- 判定：
  - mobile：**FAIL**
  - desktop：**PASS**
- 子项总判定：**FAIL**

### DoD5 总判定
- 冷启动<50ms证据链：PASS
- Lighthouse无新下降：FAIL

**最终：DoD5 = FAIL（当前仍未达成）**

补充结论（用于“可验收”决策参考）：
- 业务代码在 pages.dev 对照中未见性能退化（mobile/desktop 中位数均为100）；
- 当前阻塞点是 xgvst.com 上的 cdn-cgi jsd 注入导致的外部干扰，需先执行最小操作清单再复验主口径。
