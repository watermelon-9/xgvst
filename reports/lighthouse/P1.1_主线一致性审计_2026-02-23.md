# P1.1 主线一致性审计报告（2026-02-23）

- 任务编号：`P1.1-CHECK`
- 审计仓库：`/Users/mac/.openclaw/workspace/xgvst`
- 对照基准：`/Users/mac/.openclaw/media/inbound/file_58---6b949aef-3bf1-4393-ac8d-e0eb4fed43e1.md` 中 **1.1 条款 + DoD**
- 审计时间：2026-02-23

---

## 一、最终结论

**总判定：FAIL（未完全一致）**

> 关键原因：Lighthouse 未达 DoD（Performance ≥98）、monorepo 约定的 `packages/workers` 未落地、DoD 要求“Mock 行情支持暗黑切换”未实现显式切换能力、且 1.1 条款指定 `@unocss/svelte` 依赖未出现（当前为 `unocss/vite` 方案）。

---

## 二、重点核查结果（按你指定的5项）

### 1) UnoCSS 与金融色标（up/down/bgPurple）

**结果：部分通过（PARTIAL）**

通过项：
- `apps/web/uno.config.ts` 已定义金融色标：
  - `up: #ef4444`
  - `down: #22c55e`
  - `bgPurple: #4f46e5`
- `apps/web/src/routes/+layout.svelte` 引入 `virtual:uno.css`，`apps/web/vite.config.ts` 启用 `UnoCSS()` 插件。
- `apps/web/src/routes/+page.svelte` 已使用 `bg-bgPurple/50`、`text-up`、`text-down`，说明 UnoCSS token 在页面中有实际消费。

差异项：
- 1.1 条款文字明确写的是集成 **`@unocss/svelte`**，当前 `apps/web/package.json` 未见该依赖，实际采用 `unocss/vite`。

---

### 2) 路由是否仅 `/` `/market` `/detail/[symbol]`

**结果：PASS**

当前 `apps/web/src/routes` 文件仅有：
- `+page.svelte` -> `/`
- `market/+page.svelte` -> `/market`
- `detail/[symbol]/+page.svelte` -> `/detail/[symbol]`
- 另有 `+layout.svelte`（布局文件，不构成额外业务路由）

未发现 demo 冗余页面。

---

### 3) mock 与 `$lib/api`

**结果：PASS**

证据：
- API 抽象：`apps/web/src/lib/api/client.ts`
- Mock 数据：`apps/web/src/lib/api/mock.ts`
- 类型：`apps/web/src/lib/api/types.ts`
- MSW handlers：`apps/web/src/lib/mocks/handlers.ts`
- 客户端挂载：`apps/web/src/hooks.client.ts`（DEV 且 `PUBLIC_ENABLE_MSW=true` 才启动）

---

### 4) check / build

**结果：PASS**

执行结果：
- `corepack pnpm check`：通过（`svelte-check found 0 errors and 0 warnings`）
- `corepack pnpm build`：通过（Cloudflare adapter 构建完成）

---

### 5) Lighthouse 与 dev 启动

**结果：FAIL（DoD 未达）**

#### dev 启动
- 命令：`corepack pnpm --filter web dev --host 127.0.0.1 --port 4173`
- 输出：`VITE v7.3.1 ready in 742 ms`
- 对照 DoD：`pnpm dev 秒开（<800ms）`
- 判定：**PASS**

#### Lighthouse（本次复测）
- 命令：
  - `npx -y lighthouse http://127.0.0.1:4173/ --quiet --chrome-flags='--headless=new --no-sandbox' --only-categories=performance,accessibility,best-practices,seo --output=json --output-path=reports/lighthouse/p1.1-check-2026-02-23.json`
- 结果：
  - Performance: **72**
  - Accessibility: 94
  - Best Practices: 100
  - SEO: 100
  - FCP: 3.2s
  - LCP: 5.9s
  - TBT: 20ms
  - CLS: 0
- 对照 DoD：`Lighthouse Performance ≥ 98（移动+桌面）`
- 判定：**FAIL**

---

## 三、1.1 条款与 DoD 全量差异清单

### A. 条款级差异

1. **monorepo 结构差异（FAIL）**
   - 要求：`apps/web + packages/workers + pnpm-workspace.yaml`
   - 现状：`pnpm-workspace.yaml` 存在，`apps/web` 存在，但 `packages/workers` 未创建（`packages/` 为空）。

2. **UnoCSS 集成实现差异（PARTIAL）**
   - 要求文字：`@unocss/svelte`
   - 现状：`unocss/vite` + `virtual:uno.css`，可工作但与条款字面不完全一致。

### B. DoD 级差异

1. **DoD-2（Mock 行情支持暗黑切换）未满足（FAIL）**
   - 现状：页面为默认暗色风格，未发现显式“暗黑切换开关/切换逻辑”。

2. **DoD-4（Lighthouse Performance ≥ 98）未满足（FAIL）**
   - 本次实测 Perf=61，不达标。

### C. 已满足项

- DoD-1：`pnpm dev <800ms`（本次 660ms）
- DoD-3：Svelte 5 Runes 使用存在，`check` 无 warning
- DoD-5：`PERFORMANCE.md` 与初始报告文件已存在

---

## 四、PASS/FAIL 汇总表

| 项目 | 结论 |
|---|---|
| UnoCSS + 金融色标保留并生效 | PARTIAL |
| 路由仅 `/` `/market` `/detail/[symbol]` | PASS |
| mock 与 `$lib/api` | PASS |
| `pnpm check` / `pnpm build` | PASS |
| `pnpm dev` 秒开 | PASS |
| Lighthouse（DoD阈值） | FAIL |
| 1.1 条款全量一致性（含 workers / @unocss/svelte / DoD） | **FAIL** |

---

## 五、审计产物

- 本报告：`reports/lighthouse/P1.1_主线一致性审计_2026-02-23.md`
- Lighthouse 原始结果：`reports/lighthouse/p1.1-check-2026-02-23.json`

