# P1.3-B2 API多源抽象收口报告（2026-02-24）

## 1) 目标与范围
- 范围限定：`/Users/mac/.openclaw/workspace/xgvst`
- 目标：完成 `$lib/api` 多源抽象最终收口
  - provider 可切换：`workers | alltick | sina | tencent | mock`
  - 默认 `workers`
  - 请求失败可回退 `mock`
  - 接口命名与目录结构稳定、可扩展
  - 补充 `.env.example` 注释
  - 执行 `pnpm check` + `pnpm build`

## 2) 实施内容

### 2.1 多 provider 抽象收口
- 统一 provider 注册与切换：
  - `apps/web/src/lib/api/config.ts`
  - `apps/web/src/lib/api/providers/index.ts`
- provider 列表完整覆盖：
  - `workers/alltick/sina/tencent/mock`
- 默认 provider：`workers`
- 失败回退：`apiConfig.fallbackToMock=true` 时统一回退到 `mock`

### 2.2 接口命名稳定化
- provider 接口统一为：
  - `fetchUniverse()`
  - `fetchQuote()`
- 客户端导出：
  - `fetchUniverse`
  - `fetchQuote`
  - 保留 `fetchQuoteMock`（deprecated alias）以兼容旧调用

### 2.3 目录结构稳定化
- API 目录采用分层结构：
  - `api/config.ts`（配置解析）
  - `api/client.ts`（统一入口 + fallback）
  - `api/providers/*`（provider 实现）
  - `api/index.ts`（稳定导出门面）
- 页面调用切到稳定门面：
  - `import { fetchUniverse } from '$lib/api'`

### 2.4 环境变量示例注释完善
- `apps/web/.env.example`：补充 provider 切换、默认值、fallback 说明
- 根 `.env.example`：补充根级变量用途说明，并补充 web 公开变量示例与注释

## 3) 关键变更文件
- `.env.example`
- `apps/web/.env.example`
- `apps/web/src/lib/api/client.ts`
- `apps/web/src/lib/api/config.ts`
- `apps/web/src/lib/api/index.ts`
- `apps/web/src/lib/api/providers/index.ts`
- `apps/web/src/lib/api/providers/types.ts`
- `apps/web/src/lib/api/providers/http.ts`
- `apps/web/src/lib/api/providers/workers.ts`
- `apps/web/src/lib/api/providers/alltick.ts`
- `apps/web/src/lib/api/providers/sina.ts`
- `apps/web/src/lib/api/providers/tencent.ts`
- `apps/web/src/lib/api/providers/mock.ts`
- `apps/web/src/routes/market/+page.svelte`

## 4) 验证结果

### 4.1 `pnpm check`
- 命令：`corepack pnpm check`
- 结果：通过
- 摘要：`svelte-check found 0 errors and 0 warnings`

### 4.2 `pnpm build`
- 命令：`corepack pnpm build`
- 结果：通过
- 摘要：SvelteKit client/server build 完成，adapter-cloudflare 输出成功

## 5) DoD 状态
| DoD 项 | 状态 | 说明 |
|---|---|---|
| provider 可切换（workers/alltick/sina/tencent/mock） | ✅ | 配置解析 + provider 注册完成 |
| 默认 workers | ✅ | `PUBLIC_API_PROVIDER` 非法/缺省回落 `workers` |
| 失败回退 mock | ✅ | `withFallback` 统一捕获并按开关回退 |
| 接口命名/目录稳定可扩展 | ✅ | 统一 `fetchQuote` + `api/index.ts` 门面导出 |
| `.env.example` 注释补充 | ✅ | 根与 web 两处示例均补充 |
| `pnpm check` + `pnpm build` | ✅ | 均已通过 |

## 6) 提交信息
- 提交说明：`feat(web): finalize P1.3-B2 multi-provider api abstraction`
- 仅包含 P1.3-B2 相关改动：是（仅 API 多源抽象、env 注释、market 页调用收口与本报告）
