# P1.3-CLOSE-B 收口报告（2026-02-24）

## 第一段｜按《修订版》执行结果（对应 P1.3「详细步骤」）

本次仅聚焦 DoD2 收口：**SvelteKit 前端经 `$lib/api` 真实调用 Workers 接口并展示 Mock 行情（非 fallback 假成功）**。

已完成：

1. **Workers 对齐前端调用路径**
   - 文件：`packages/workers/src/index.ts`
   - 新增并确认可用：`GET /v3/universe`（返回 boards + watchlist Mock 数据）
   - 保持可用：`GET /api/quote/mock`
   - 补齐 CORS（`Access-Control-Allow-Origin` 等），支持浏览器跨源联调（web:4173 → workers:8791 / workers.dev）

2. **前端 `$lib/api` 环境变量接入修正（关键）**
   - 文件：`apps/web/src/lib/api/config.ts`
   - 从 `import.meta.env.PUBLIC_*` 改为 `env`（`$env/dynamic/public`），避免 PUBLIC 变量在客户端取值异常导致“未发请求即失败”。
   - 直接效果：`workersProvider.fetchUniverse()` 可拿到真实 `PUBLIC_WORKER_API_URL`。

3. **provider/fallback 默认策略收口**
   - 文件：
     - `apps/web/src/lib/api/config.ts`
     - `apps/web/.env.example`
     - `.env.example`
   - 默认保持 `PUBLIC_API_PROVIDER=workers`
   - 默认改为 `PUBLIC_API_FALLBACK_MOCK=false`（联调期显式关闭 fallback，防止掩盖真实接口问题）。

4. **Worker 生产更新**
   - 执行：`corepack pnpm --filter workers run deploy`
   - 发布 URL：`https://xgvst-workers.viehh642.workers.dev`
   - Version ID：`da7702a1-5802-4538-8b48-b3de5053e815`

---

## 第二段｜修订版注意事项落地（对应 P1.3「注意事项」）

1. **前后端边界**：继续使用独立 `packages/workers`，未将 SvelteKit `+server.ts` 作为生产 Worker。✅
2. **抽象层统一入口**：前端页面仍通过 `$lib/api` 门面（`fetchUniverse`）调用，未出现页面直连硬编码 URL。✅
3. **联调一致性**：完成 `wrangler dev` + `web dev` 双开联调；并在浏览器真实访问验证。✅
4. **避免 fallback 假绿**：默认 fallback 改为 false，并在联调中显式 `false` 验证，确保失败即暴露、成功即真实命中 Workers。✅

---

## 第三段｜DoD2 证据链（请求日志 + curl + 浏览器）

证据目录：`reports/lighthouse/P1.3_CLOSE_B_evidence/`

### A) curl 证据（本地联调）
- 文件：`curl-workers-local.txt`
- 关键结果：
  - `GET http://127.0.0.1:8791/v3/universe` → `HTTP/1.1 200 OK`
  - `GET http://127.0.0.1:8791/api/quote/mock` → `HTTP/1.1 200 OK`

### B) curl 证据（生产 Workers）
- 文件：`curl-workers-deployed.txt`
- 关键结果：
  - `GET https://xgvst-workers.viehh642.workers.dev/v3/universe` → `HTTP/2 200`
  - `GET https://xgvst-workers.viehh642.workers.dev/api/quote/mock` → `HTTP/2 200`

### C) 浏览器证据（真实页面 + 网络请求）
- 文件：
  - 截图：`browser-market-proof.png`
  - 网络与页面文本：`browser-proof.json`
- 关键结果（来自 `browser-proof.json`）：
  - 浏览器发起请求：`GET http://127.0.0.1:8791/v3/universe`
  - 响应状态：`200`
  - 页面文本出现实际行情内容：
    - 版块：`沪深主板 / 人工智能 / 新能源`
    - 自选：`平安银行(000001) / 贵州茅台(600519) / 宁德时代(300750)`
  - `errors: []`（无前端报错）

### D) 请求日志证据（Workers dev）
- 来自 `wrangler dev` 实时日志：
  - `GET /v3/universe 200 OK`
  - 多次请求命中（含浏览器联调触发）

---

## 第四段｜DoD判定与剩余风险

### DoD2 判定
- 目标：**SvelteKit 前端通过 `$lib/api` 真实调用 Workers 接口并可显示 Mock 行情（不是仅 fallback）**
- 结果：**✅ PASS**

### 关键判据
- fallback 关闭时（`PUBLIC_API_FALLBACK_MOCK=false`）页面仍可显示行情。
- 浏览器网络证据明确命中 `/v3/universe` 并返回 200。
- Workers 日志与 curl 结果双向一致。

### 说明
- `pnpm --filter web build` 当前受既有 PWA 依赖问题（`workbox-window` 解析失败）影响，属于仓库既存构建项，不影响本次 DoD2 联调判定；本次收口以 **接口真实联调证据** 为准。
