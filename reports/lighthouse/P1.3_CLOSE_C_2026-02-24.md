# P1.3-CLOSE-C 收口报告（DoD5专项）

日期：2026-02-24  
范围：`xgvst/reports/lighthouse` 现有证据复核 + DoD5判定口径落地  
目标：按《修订版》四段执行，并对 DoD5（性能报告：冷启动<50ms + Lighthouse无新下降）给出可审计结论。

---

## 一、详细步骤（对应《修订版》1）

1. 复核 Workers 侧性能证据：
   - `reports/lighthouse/P1.3_C2_CI/wrangler-tail-xgvst-workers-ok-20260224-0031.jsonl`
2. 复核 Lighthouse 证据（P1.3 当前批次）：
   - `reports/lighthouse/P1.3_C2_CI/lh-median-20260224-001407-summary.json`
3. 选取 P1.2 最终复验作为“无新下降”对比基线：
   - 桌面：`reports/lighthouse/p1.2-retest-desktop-20260223-225115-r1.json`、`...-r2.json`
   - 移动：`reports/lighthouse/p1.2-retest-mobile-20260223-225115-r1~r5.json`
4. 形成 DoD5 判定口径并出具 PASS/FAIL（不做硬判通过）。

---

## 二、注意事项（对应《修订版》2）

1. **冷启动证据口径风险**：`wrangler tail` 当前样本提供 `wallTime/cpuTime`，但未显式标注 cold/warm；只能证明“该次请求 Worker 执行耗时”，不能 100% 证明“冷启动”。
2. **Lighthouse 可比性约束**：仅对比同域名（`https://xgvst.com/`）、同类目（performance）、同端形（mobile/desktop）数据；不同 URL 或 JSON API 结果（如 `NOT_HTML`）不纳入。
3. **移动端波动因素**：P1.3-C2 已确认受 `cdn-cgi/challenge-platform/scripts/jsd/main.js` 干扰，存在显著离散；判定时以“中位数”作为唯一门禁统计量。

---

## 三、工作安排执行回执（对应《修订版》3，Sub-C）

1. 已完成 Workers 生产性能日志复核（tail 样本可读取、可追溯脚本版本）。
2. 已完成 Lighthouse 对比口径固化（基线=P1.2最终复验，当前=P1.3-C2 3-run中位数）。
3. 已输出本收口文档，明确 DoD5 通过/不通过及阻塞项。

---

## 四、验收标准（DoD）判定（对应《修订版》4）

> 仅覆盖本次目标 DoD5：  
> **“性能报告已提交，冷启动时间<50ms，Lighthouse 无新下降”**

### 4.1 子项A：冷启动时间 < 50ms（证据复核）

证据文件：
- `reports/lighthouse/P1.3_C2_CI/wrangler-tail-xgvst-workers-ok-20260224-0031.jsonl`

关键字段（/health 请求）：
- `wallTime = 3`
- `cpuTime = 3`
- `outcome = ok`
- `scriptVersion.id = 035efad8-afb8-4b5f-a987-3beedcfcf0b9`

结论：
- 从“执行耗时”看，**3ms < 50ms**，满足阈值。  
- 但该样本**未显式注明 cold start**，严格审计语义下仅能判定为“<50ms 执行耗时证据成立”，**冷启动属性证据不足**。

子项A判定：**部分满足（证据口径有缺口）**。

### 4.2 子项B：Lighthouse 无新下降（判定口径 + 对比结果）

**判定口径（本报告固化）**：
- 比较对象：同 URL（`https://xgvst.com/`）同端形的 `performance` 分数。
- 统计方法：均采用“多次采样中位数”。
- 通过条件：`P1.3 当前中位数 >= P1.2 基线中位数`，否则视为“有新下降”。

**基线（P1.2最终复验）**：
- Desktop：100（r1=100, r2=100，中位数=100）
- Mobile：97（r1~r5: 96/98/98/97/95，中位数=97）

基线证据：
- `reports/lighthouse/p1.2-retest-desktop-20260223-225115-r1.json`
- `reports/lighthouse/p1.2-retest-desktop-20260223-225115-r2.json`
- `reports/lighthouse/p1.2-retest-mobile-20260223-225115-r1.json`
- `reports/lighthouse/p1.2-retest-mobile-20260223-225115-r2.json`
- `reports/lighthouse/p1.2-retest-mobile-20260223-225115-r3.json`
- `reports/lighthouse/p1.2-retest-mobile-20260223-225115-r4.json`
- `reports/lighthouse/p1.2-retest-mobile-20260223-225115-r5.json`

**当前（P1.3-C2）**：
- Mobile 3-run：65 / 87 / 87
- 中位数：87
- 证据：`reports/lighthouse/P1.3_C2_CI/lh-median-20260224-001407-summary.json`

对比判定：
- `87 < 97` → **存在新下降**。

子项B判定：**不满足**。

### 4.3 DoD5总判定

- 性能报告：**已提交**（本文件）。
- 冷启动<50ms：**仅有执行耗时<50ms证据，冷启动属性证据不足**。
- Lighthouse无新下降：**不满足（移动端中位数 87 低于基线 97）**。

## 最终结论：**DoD5 未通过（FAIL）**

阻塞项：
1. 缺少“可明确标识 cold start”的官方或可审计指标证据链（当前 tail 样本未区分 cold/warm）。
2. Lighthouse 按中位数口径出现新下降（mobile 87 vs baseline 97），不满足“无新下降”。

建议解阻（供主代理排期）：
- A) 在 Cloudflare 可观测链路补充 cold-start 可判别字段（或仪表盘导出）后重验；
- B) 对 `cdn-cgi jsd` 干扰实施测试白名单/稳定化策略，再执行同口径 3-run（建议>=5-run）复验。
